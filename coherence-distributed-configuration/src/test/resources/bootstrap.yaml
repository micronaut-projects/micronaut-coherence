micronaut:
  application:
    name: hello-world
  config-client:
    enabled: true

coherence:
  cluster: test-cluster
  role: test
  ttl: 0
  distributed:
    localstorage: true
  sessions:
    config:
      type: server
      config: coherence-cache-config.xml
  client:
    enabled: false
    # gRPC config:
    host: localhost
    port: 0
